[project]
name = "safety"
version = "0.1.0"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "matplotlib",
    "pytest"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["src"]